[metadata]
name = capturebot
version = 0.0.1

[options]
packages = capturebot
install_requires =
	python-telegram-bot

# [options.package_data]
# capturebot=data/*

[options.entry_points]
console_scripts=
	capturebot = capturebot:main